version: '3'

services:
  web:
    build: ./
    ports:
      - "3001:3000"
    environment:
      - API_ENDPOINT=http://api:4567/api/
      - MONGOPORT=27017
    links:
      - mongodb
  mongodb: 
    image: mvertes/alpine-mongo